import 'package:flutter/material.dart';

class Settings {
  String title;
  String subtitle;
  IconData icon;
  String route;

  Settings({
    required this.title,
    required this.icon,
    required this.route,
    required this.subtitle,

  });
}

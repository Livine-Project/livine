import 'package:flutter/material.dart';

class ColorTypes {
  String type;
  String subtitle;
  Color color;
  ColorTypes({
    required this.type,
    required this.color,
    required this.subtitle,
  });
}
